{% extends('mail/layout.txt.twig') %}
{% block content %}

{% trans %} hello {% endtrans %} {{ nickname }}!
{{ absolute_url(path('token_show', {'name': rewardToken })) }}
{% trans %} mail.reward.volunteer.rejected.msg {% endtrans %} 
{{ absolute_url(path('token_show', {'name': rewardToken, 'tab': 'intro', 'modal': 'reward-finalize', 'slug': slug })) }}
{% endblock %}

