twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'form/fields.html.twig'
    globals:
        global_youtube_client_id: '%youtube_client_id%'
        global_facebook_app_id: '%facebook_app_id%'
        version: '%version%'
        global_websocket_url: '`ws://mintme.abchosting.org:8364`'
