twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    form_themes:
        - 'form/fields.html.twig'
    globals:
        users_count_refresh_rate: '%users_count_refresh_rate_seconds%'
        mintme_launch_date: '%mintme_launch_date%'
        referral_percentage: '%referral_percentage%'
        referral_free_trading_date: '%referral_free_trading_date%'
    